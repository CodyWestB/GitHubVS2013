@model www.CodyBerndt.com.Models.About.AboutModels

@{
    ViewBag.Title = "About";
}

@*<hgroup class="title">
        <h1>@ViewBag.Title.</h1>
        <h2>@ViewBag.Message</h2>
    </hgroup>

    <article>
    </article>*@

<div class="container">
    <div id="carousel-halo-generic" class="carousel" data-ride="carousel" data-interval="5000">

        <!-- Plan: turn all of the image file names into a list
            Do a loop to add an indicator for each (count)
            then add the name of each to a for loop that builds each slide with image name

           *** Requires implementation of angularJS files

            -->
        <!-- Indicators -->
        <ol class="carousel-indicators">
            <li data-target="#carousel-halo-generic" data-slide-to="0" class="active"></li>
            @for (int i = 1; i < @Model.HaloImages.Count; i++)
            {
                <li data-target="#carousel-halo-generic" data-slide-to="@i"></li>
            }
            @*<li data-target="#carousel-halo-generic" data-slide-to="1"></li>
                <li data-target="#carousel-halo-generic" data-slide-to="2"></li>*@
        </ol>
        <!-- Wrapper for slides -->
        <div class="carousel-inner" role="listbox">
                
            @*//this gets you the item (item.value) and its index (item.i)*@
            @{
                foreach (var image in Model.HaloImages.Select((value, i) => new {i, value}))
                 {
                     //The index is @item.i and the value is @item.value.DisplayName
                         @*<text>*@
                             <div class="item" ng-class="active: @image.i != 0">
                                <img src="/Images/Halo/@image.value" alt="...">
                                <div class="carousel-caption">
                                    ...
                                </div>
                            </div>
                         @*</text>*@
                 }
            }
            @*<div class="item active">
                    <img src="/Images/Halo/Concept_Art_Forerunner_AI.jpg" alt="...">
                    <div class="carousel-caption">
                        ...
                    </div>
                </div>
                <div class="item">
                    <img src="/Images/Halo/Art_of_Scoping_Halo_Landscape.jpg" alt="...">
                    <div class="carousel-caption">
                        ...
                    </div>
                </div>*@
            ...
        </div>
        <!-- Controls -->
        <a class="left carousel-control" href="#carousel-halo-generic" role="button" data-slide="prev">
            <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="right carousel-control" href="#carousel-halo-generic" role="button" data-slide="next">
            <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>

</div>





<aside>
    <h3>Aside Title</h3>
    <p>
        Use this area to provide additional information.
    </p>
    <ul>
        <li>@Html.ActionLink("Home", "Index", "Home")</li>
        <li>@Html.ActionLink("About", "About", "About")</li>
        <li>@Html.ActionLink("Contact", "Contact", "Contact")</li>
    </ul>
</aside>